# 数据源说明

## MVP 选择

本项目 MVP 采用三类国内 API 组合：

```text
高德地图 -> 周边酒店候选池
FlyAI/飞猪 -> 本店和候选酒店价格
百度地图 -> 评论数、细分评分和图片数等口碑补充
本地历史库 -> 同口径昨日对比
```

这版不把携程、飞猪、美团等网页端浏览器自动化作为主流程。原因很直接：网页端读取慢、容易遇到登录墙和风控，也会让 WorkBuddy 花大量时间看页面。API 组合版优先把“候选发现、价格、口碑”三件事拆开，用更稳定的结构生成日报输入。

## 高德作为主地图源

高德适合承担本店定位和候选池发现：

- 用本店名称做文本检索，拿到地址、坐标、类型、商圈、评分和档位标签。
- 用本店坐标做周边检索，拉出 1km、2km、3km 内住宿类 POI。
- 周边结果自带距离，便于筛出真正会抢同一批客人的酒店。
- 坐标和 FlyAI 返回坐标通常更容易对齐。

高德不作为价格源，也不强求它提供评论数。它的职责是回答：

```text
本店在哪里？附近有哪些可能相关的住宿？谁离我最近？谁同档或相邻档？
```

## FlyAI/飞猪作为价格源

FlyAI/飞猪适合查询入住日期下的价格信号：

- 查询本店价格。
- 查询候选竞品价格。
- 返回酒店名、地址、坐标、品牌、档位、飞猪详情链接和价格。

注意：FlyAI/飞猪价格只代表飞猪生态价格，不代表全 OTA 最低价。如果没有有效 Key，价格可能变成 `¥1xx`、`¥3x` 这种脱敏结果，只能作为价格带参考。

为了降低风控和解释数据缺口，项目会输出 `## FlyAI Usage`：

- `flyai.requestDelayMs`：正式运行时每次 FlyAI 请求前等待，默认 800ms。
- `flyai.maxRetries`：失败重试次数，默认 1 次。
- `ExternalCallsUsed`：真实 FlyAI 调用次数，DryRun 为 0。
- `EmptyResults`、`Failed`、`MaskedPrices`：分别表示空结果、失败、脱敏价格数量。

日报看到空结果、失败或脱敏价格时，不能当作精确价格判断。

## 百度作为口碑补充源

百度作为口碑补充源，不负责全量扫酒店，也不负责价格。建议只对入围的 10-20 家候选酒店补充：

- 评论数。
- 总评分。
- 设施评分。
- 卫生评分。
- 服务评分。
- 图片数。
- 品牌和档位。

这些字段用来判断“品质压力”：不是谁离得近、谁便宜就是核心竞品，高评分、高评论数、设施/卫生/服务更强的酒店，也可能对本店形成品质压力。

为了省百度额度，项目默认启用本地缓存：

- `baidu.cacheEnabled`：默认 `true`。
- `baidu.cacheDirectory`：默认 `data/cache/baidu`。
- `baidu.cacheTtlDays`：默认 30 天。
- `baidu.dailyCallLimit`：默认每天最多 20 次真实百度 HTTP 调用。

命中缓存时不会调用百度 API。DryRun 不消耗真实百度额度。日报输入里的 `## Baidu Usage` 会显示缓存命中、真实调用次数和被限额跳过数量。

## 本地历史库用于昨日对比

正式运行脚本时，会把当日结果保存为：

```text
data/history/YYYY-MM-DD.json
```

历史快照只保存在本机，默认不会提交 GitHub。脚本会用城市、本店、POI、入住口径、房型、房间数、住客数等生成查询指纹，只和同口径的上一份历史快照比较。

有历史时，日报输入会生成 `## Yesterday Comparison`，用于判断：

- 谁涨价了。
- 谁降价了。
- 谁持平。
- 本店和竞品价差有没有扩大。
- 口碑评论数是否有明显变化。

没有同口径历史时，不判断“谁调价了”，只做今日横截面分析。

## 分层规则

分层规则可以在 `config/hotel-monitor.json` 的 `tierRules` 中调整：

- `coreRadiusMeters`：核心竞品半径。
- `pricePressureRatio`：价格压力比例，默认低于本店 75% 算价格压力。
- `qualityRatingThreshold`：品质压力评分阈值。
- `qualityRadiusMeters`：品质压力可比半径。
- `includeAlternativeLodging`：是否把公寓、民宿、电竞酒店等作为替代竞品保留。

默认规则：

- 核心竞品：距离近、档位接近、价格重叠、标准酒店、评分和评论数可信。
- 价格压力：距离近但价格明显更低，会分流价格敏感客人。
- 品质压力：价格接近或略高，但评分、评论数、设施/卫生/服务更强。
- 替代竞品：距离近但业态不同，例如酒店式公寓、民宿、电竞酒店、青年旅舍。
- 剔除：停车场、大堂、写字楼、商场入口、非住宿 POI、距离太远或名称明显不匹配。

## 安全边界

- `AMAP_API_KEY`、`FLYAI_API_KEY`、`BAIDU_MAP_AK` 只从 Windows 环境变量读取。
- 不把 Key 写进 Markdown、代码、配置示例、报告或 GitHub。
- `config/hotel-monitor.json`、`data/`、`reports/` 都是本地私有文件，默认已被 `.gitignore` 忽略。
